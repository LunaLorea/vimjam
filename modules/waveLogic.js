function generateEnemyPattern(round) {
    // how long is a wave? → dep on pattern generated, plus hard limit
    // no long brakes
    // overtime increase amounts, allow more types, decrease entropy
    // how do we trigger the spawn of an enemy?
}